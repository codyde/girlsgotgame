import { Router } from 'express';

const router = Router();

// R2 upload routes removed - using UploadThing instead
// All upload functionality now handled by UploadThing at /api/uploadthing

export default router;